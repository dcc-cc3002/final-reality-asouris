package weapon.traits
import weapon.Weapon

/**
 * A trait representing a weapon that can be equipped by a warrior character.
 *
 * This trait extends the Weapon trait, hence it inherits properties and behaviors of a weapon.
 */
trait EquippableByWarrior extends Weapon{
}
